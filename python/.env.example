# ===========================================================================
# AutoGen 模型配置示例文件
# ===========================================================================
# 使用说明：
# 1. 复制本文件为 .env：cp .env.example .env
# 2. 填写你的 API Key 和其他配置
# 3. .env 文件已在 .gitignore 中排除，不会被提交到版本库
# ===========================================================================

# ---------------------------------------------------------------------------
# 阿里千问（DashScope）云端模型 - 需要 API Key
# ---------------------------------------------------------------------------
# 获取方式：https://dashscope.console.aliyun.com/apiKey
DASHSCOPE_API_KEY=your_dashscope_api_key_here

# 可选：指定使用的模型（默认：qwen-plus）
# 可选值：qwen-plus, qwen-max, qwen-turbo, qwen-long 等
# DASHSCOPE_MODEL=qwen-plus

# 可选：自定义 API 端点（通常不需要修改）
# DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ---------------------------------------------------------------------------
# Ollama 本地模型 - 无需 API Key，完全本地运行
# ---------------------------------------------------------------------------
# 前置条件：
# 1. 安装 Ollama：https://ollama.ai/
# 2. 拉取模型：ollama pull qwen3:4b
# 3. 确认服务运行：访问 http://localhost:11434

# 可选：指定使用的模型（默认：qwen3:4b）
# 其他选项：llama2, mistral, codellama, neural-chat 等
# OLLAMA_MODEL=qwen3:4b

# 可选：自定义 Ollama API 端点（默认：http://localhost:11434/v1）
# 如果 Ollama 运行在其他端口或远程服务器，可以修改此配置
# OLLAMA_BASE_URL=http://localhost:11434/v1

# ---------------------------------------------------------------------------
# 其他模型配置（示例，需要在 model_clients_registry.py 中实现）
# ---------------------------------------------------------------------------

# OpenAI（如果需要）
# OPENAI_API_KEY=sk-xxx
# OPENAI_MODEL=gpt-4
# OPENAI_BASE_URL=https://api.openai.com/v1

# 智谱 AI GLM（已从注册表移除，如需使用请参考 DEPLOYMENT.md）
# ZHIPU_API_KEY=your_zhipu_api_key
# ZHIPU_MODEL=glm-4-plus
# ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4/

# DeepSeek（如果需要）
# DEEPSEEK_API_KEY=your_deepseek_api_key
# DEEPSEEK_MODEL=deepseek-chat
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# ---------------------------------------------------------------------------
# 高级配置
# ---------------------------------------------------------------------------

# HTTP 代理配置（如果需要）
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080

# 日志级别（可选：DEBUG, INFO, WARNING, ERROR）
# LOG_LEVEL=INFO
